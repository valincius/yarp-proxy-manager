# Upstream: answers "hello" on every path so /hello always returns 200.
server {
    listen 80;
    server_name _;
    location / {
        default_type text/plain;
        return 200 "benchmark-upstream\n";
    }
}
