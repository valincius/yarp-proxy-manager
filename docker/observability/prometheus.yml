# Prometheus scrape config for YARP Proxy Manager.
global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: yarp-proxy-manager
    metrics_path: /metrics
    static_configs:
      - targets: ['proxy-manager:81']
